import text from text file
sanitize

heapify text string by ascii representation
extract min until heap is empty, counting # of occurances of each char

put each char in heap with freq as key and char as data

hashtable
key
char
huffman code
freq